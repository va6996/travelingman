// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.0
//   protoc               v5.29.2
// source: service/itinerary.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";

export const protobufPackage = "travelingman.service";

export interface ItineraryItem {
  id: number;
  groupId: number;
  /** "flight", "hotel", "activity", "note" */
  type: string;
  title: string;
  startTime: Date | undefined;
  endTime:
    | Date
    | undefined;
  /** Flexible meta-data */
  detailsJson: Uint8Array;
}

export interface CreateItineraryItemRequest {
  groupId: number;
  type: string;
  title: string;
  /** RFC3339 */
  startTime: string;
  /** RFC3339 */
  endTime: string;
  detailsJson: Uint8Array;
}

export interface GetItineraryItemsRequest {
  groupId: number;
}

export interface GetItineraryItemsResponse {
  items: ItineraryItem[];
}

export interface DeleteItineraryItemRequest {
  itemId: number;
}

function createBaseItineraryItem(): ItineraryItem {
  return {
    id: 0,
    groupId: 0,
    type: "",
    title: "",
    startTime: undefined,
    endTime: undefined,
    detailsJson: new Uint8Array(0),
  };
}

export const ItineraryItem: MessageFns<ItineraryItem> = {
  encode(message: ItineraryItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int64(message.id);
    }
    if (message.groupId !== 0) {
      writer.uint32(16).int64(message.groupId);
    }
    if (message.type !== "") {
      writer.uint32(26).string(message.type);
    }
    if (message.title !== "") {
      writer.uint32(34).string(message.title);
    }
    if (message.startTime !== undefined) {
      Timestamp.encode(toTimestamp(message.startTime), writer.uint32(42).fork()).join();
    }
    if (message.endTime !== undefined) {
      Timestamp.encode(toTimestamp(message.endTime), writer.uint32(50).fork()).join();
    }
    if (message.detailsJson.length !== 0) {
      writer.uint32(58).bytes(message.detailsJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ItineraryItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseItineraryItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.groupId = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.startTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.endTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.detailsJson = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ItineraryItem {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      groupId: isSet(object.groupId) ? globalThis.Number(object.groupId) : 0,
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      startTime: isSet(object.startTime) ? fromJsonTimestamp(object.startTime) : undefined,
      endTime: isSet(object.endTime) ? fromJsonTimestamp(object.endTime) : undefined,
      detailsJson: isSet(object.detailsJson) ? bytesFromBase64(object.detailsJson) : new Uint8Array(0),
    };
  },

  toJSON(message: ItineraryItem): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.groupId !== 0) {
      obj.groupId = Math.round(message.groupId);
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.startTime !== undefined) {
      obj.startTime = message.startTime.toISOString();
    }
    if (message.endTime !== undefined) {
      obj.endTime = message.endTime.toISOString();
    }
    if (message.detailsJson.length !== 0) {
      obj.detailsJson = base64FromBytes(message.detailsJson);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ItineraryItem>, I>>(base?: I): ItineraryItem {
    return ItineraryItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ItineraryItem>, I>>(object: I): ItineraryItem {
    const message = createBaseItineraryItem();
    message.id = object.id ?? 0;
    message.groupId = object.groupId ?? 0;
    message.type = object.type ?? "";
    message.title = object.title ?? "";
    message.startTime = object.startTime ?? undefined;
    message.endTime = object.endTime ?? undefined;
    message.detailsJson = object.detailsJson ?? new Uint8Array(0);
    return message;
  },
};

function createBaseCreateItineraryItemRequest(): CreateItineraryItemRequest {
  return { groupId: 0, type: "", title: "", startTime: "", endTime: "", detailsJson: new Uint8Array(0) };
}

export const CreateItineraryItemRequest: MessageFns<CreateItineraryItemRequest> = {
  encode(message: CreateItineraryItemRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0) {
      writer.uint32(8).int64(message.groupId);
    }
    if (message.type !== "") {
      writer.uint32(18).string(message.type);
    }
    if (message.title !== "") {
      writer.uint32(26).string(message.title);
    }
    if (message.startTime !== "") {
      writer.uint32(34).string(message.startTime);
    }
    if (message.endTime !== "") {
      writer.uint32(42).string(message.endTime);
    }
    if (message.detailsJson.length !== 0) {
      writer.uint32(50).bytes(message.detailsJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateItineraryItemRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateItineraryItemRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.groupId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.startTime = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.endTime = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.detailsJson = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateItineraryItemRequest {
    return {
      groupId: isSet(object.groupId) ? globalThis.Number(object.groupId) : 0,
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      startTime: isSet(object.startTime) ? globalThis.String(object.startTime) : "",
      endTime: isSet(object.endTime) ? globalThis.String(object.endTime) : "",
      detailsJson: isSet(object.detailsJson) ? bytesFromBase64(object.detailsJson) : new Uint8Array(0),
    };
  },

  toJSON(message: CreateItineraryItemRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0) {
      obj.groupId = Math.round(message.groupId);
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.startTime !== "") {
      obj.startTime = message.startTime;
    }
    if (message.endTime !== "") {
      obj.endTime = message.endTime;
    }
    if (message.detailsJson.length !== 0) {
      obj.detailsJson = base64FromBytes(message.detailsJson);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateItineraryItemRequest>, I>>(base?: I): CreateItineraryItemRequest {
    return CreateItineraryItemRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateItineraryItemRequest>, I>>(object: I): CreateItineraryItemRequest {
    const message = createBaseCreateItineraryItemRequest();
    message.groupId = object.groupId ?? 0;
    message.type = object.type ?? "";
    message.title = object.title ?? "";
    message.startTime = object.startTime ?? "";
    message.endTime = object.endTime ?? "";
    message.detailsJson = object.detailsJson ?? new Uint8Array(0);
    return message;
  },
};

function createBaseGetItineraryItemsRequest(): GetItineraryItemsRequest {
  return { groupId: 0 };
}

export const GetItineraryItemsRequest: MessageFns<GetItineraryItemsRequest> = {
  encode(message: GetItineraryItemsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0) {
      writer.uint32(8).int64(message.groupId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetItineraryItemsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetItineraryItemsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.groupId = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetItineraryItemsRequest {
    return { groupId: isSet(object.groupId) ? globalThis.Number(object.groupId) : 0 };
  },

  toJSON(message: GetItineraryItemsRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0) {
      obj.groupId = Math.round(message.groupId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetItineraryItemsRequest>, I>>(base?: I): GetItineraryItemsRequest {
    return GetItineraryItemsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetItineraryItemsRequest>, I>>(object: I): GetItineraryItemsRequest {
    const message = createBaseGetItineraryItemsRequest();
    message.groupId = object.groupId ?? 0;
    return message;
  },
};

function createBaseGetItineraryItemsResponse(): GetItineraryItemsResponse {
  return { items: [] };
}

export const GetItineraryItemsResponse: MessageFns<GetItineraryItemsResponse> = {
  encode(message: GetItineraryItemsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      ItineraryItem.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetItineraryItemsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetItineraryItemsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(ItineraryItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetItineraryItemsResponse {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => ItineraryItem.fromJSON(e)) : [],
    };
  },

  toJSON(message: GetItineraryItemsResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => ItineraryItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetItineraryItemsResponse>, I>>(base?: I): GetItineraryItemsResponse {
    return GetItineraryItemsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetItineraryItemsResponse>, I>>(object: I): GetItineraryItemsResponse {
    const message = createBaseGetItineraryItemsResponse();
    message.items = object.items?.map((e) => ItineraryItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDeleteItineraryItemRequest(): DeleteItineraryItemRequest {
  return { itemId: 0 };
}

export const DeleteItineraryItemRequest: MessageFns<DeleteItineraryItemRequest> = {
  encode(message: DeleteItineraryItemRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.itemId !== 0) {
      writer.uint32(8).int64(message.itemId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteItineraryItemRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteItineraryItemRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.itemId = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteItineraryItemRequest {
    return { itemId: isSet(object.itemId) ? globalThis.Number(object.itemId) : 0 };
  },

  toJSON(message: DeleteItineraryItemRequest): unknown {
    const obj: any = {};
    if (message.itemId !== 0) {
      obj.itemId = Math.round(message.itemId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteItineraryItemRequest>, I>>(base?: I): DeleteItineraryItemRequest {
    return DeleteItineraryItemRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteItineraryItemRequest>, I>>(object: I): DeleteItineraryItemRequest {
    const message = createBaseDeleteItineraryItemRequest();
    message.itemId = object.itemId ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
