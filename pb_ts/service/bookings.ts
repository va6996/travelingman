// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.0
//   protoc               v5.29.2
// source: service/bookings.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "travelingman.service";

export interface BookFlightRequest {
  /** JSON representation of the elected FlightOffer */
  flightOfferJson: Uint8Array;
  /** JSON list of TravelerInfo */
  travelersJson: Uint8Array;
  userId: number;
  /** Optional: For actual booking */
  bookingReference: string;
}

export interface BookFlightResponse {
  /** JSON response from Amadeus (booking confirmation) */
  rawJson: Uint8Array;
}

export interface BookHotelRequest {
  offerId: string;
  /** JSON list of HotelGuest */
  guestsJson: Uint8Array;
  /** JSON of HotelPayment */
  paymentJson: Uint8Array;
  userId: number;
}

export interface BookHotelResponse {
  rawJson: Uint8Array;
}

export interface BookTransferRequest {
  offerId: string;
  /** JSON list of TransferTraveler */
  travelersJson: Uint8Array;
  /** JSON of TransferPayment */
  paymentJson: Uint8Array;
  userId: number;
}

export interface BookTransferResponse {
  rawJson: Uint8Array;
}

function createBaseBookFlightRequest(): BookFlightRequest {
  return { flightOfferJson: new Uint8Array(0), travelersJson: new Uint8Array(0), userId: 0, bookingReference: "" };
}

export const BookFlightRequest: MessageFns<BookFlightRequest> = {
  encode(message: BookFlightRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.flightOfferJson.length !== 0) {
      writer.uint32(10).bytes(message.flightOfferJson);
    }
    if (message.travelersJson.length !== 0) {
      writer.uint32(18).bytes(message.travelersJson);
    }
    if (message.userId !== 0) {
      writer.uint32(24).int64(message.userId);
    }
    if (message.bookingReference !== "") {
      writer.uint32(34).string(message.bookingReference);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BookFlightRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBookFlightRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.flightOfferJson = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.travelersJson = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.userId = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.bookingReference = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BookFlightRequest {
    return {
      flightOfferJson: isSet(object.flightOfferJson) ? bytesFromBase64(object.flightOfferJson) : new Uint8Array(0),
      travelersJson: isSet(object.travelersJson) ? bytesFromBase64(object.travelersJson) : new Uint8Array(0),
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      bookingReference: isSet(object.bookingReference) ? globalThis.String(object.bookingReference) : "",
    };
  },

  toJSON(message: BookFlightRequest): unknown {
    const obj: any = {};
    if (message.flightOfferJson.length !== 0) {
      obj.flightOfferJson = base64FromBytes(message.flightOfferJson);
    }
    if (message.travelersJson.length !== 0) {
      obj.travelersJson = base64FromBytes(message.travelersJson);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.bookingReference !== "") {
      obj.bookingReference = message.bookingReference;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BookFlightRequest>, I>>(base?: I): BookFlightRequest {
    return BookFlightRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BookFlightRequest>, I>>(object: I): BookFlightRequest {
    const message = createBaseBookFlightRequest();
    message.flightOfferJson = object.flightOfferJson ?? new Uint8Array(0);
    message.travelersJson = object.travelersJson ?? new Uint8Array(0);
    message.userId = object.userId ?? 0;
    message.bookingReference = object.bookingReference ?? "";
    return message;
  },
};

function createBaseBookFlightResponse(): BookFlightResponse {
  return { rawJson: new Uint8Array(0) };
}

export const BookFlightResponse: MessageFns<BookFlightResponse> = {
  encode(message: BookFlightResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rawJson.length !== 0) {
      writer.uint32(10).bytes(message.rawJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BookFlightResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBookFlightResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rawJson = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BookFlightResponse {
    return { rawJson: isSet(object.rawJson) ? bytesFromBase64(object.rawJson) : new Uint8Array(0) };
  },

  toJSON(message: BookFlightResponse): unknown {
    const obj: any = {};
    if (message.rawJson.length !== 0) {
      obj.rawJson = base64FromBytes(message.rawJson);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BookFlightResponse>, I>>(base?: I): BookFlightResponse {
    return BookFlightResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BookFlightResponse>, I>>(object: I): BookFlightResponse {
    const message = createBaseBookFlightResponse();
    message.rawJson = object.rawJson ?? new Uint8Array(0);
    return message;
  },
};

function createBaseBookHotelRequest(): BookHotelRequest {
  return { offerId: "", guestsJson: new Uint8Array(0), paymentJson: new Uint8Array(0), userId: 0 };
}

export const BookHotelRequest: MessageFns<BookHotelRequest> = {
  encode(message: BookHotelRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.offerId !== "") {
      writer.uint32(10).string(message.offerId);
    }
    if (message.guestsJson.length !== 0) {
      writer.uint32(18).bytes(message.guestsJson);
    }
    if (message.paymentJson.length !== 0) {
      writer.uint32(26).bytes(message.paymentJson);
    }
    if (message.userId !== 0) {
      writer.uint32(32).int64(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BookHotelRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBookHotelRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.offerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.guestsJson = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.paymentJson = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.userId = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BookHotelRequest {
    return {
      offerId: isSet(object.offerId) ? globalThis.String(object.offerId) : "",
      guestsJson: isSet(object.guestsJson) ? bytesFromBase64(object.guestsJson) : new Uint8Array(0),
      paymentJson: isSet(object.paymentJson) ? bytesFromBase64(object.paymentJson) : new Uint8Array(0),
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: BookHotelRequest): unknown {
    const obj: any = {};
    if (message.offerId !== "") {
      obj.offerId = message.offerId;
    }
    if (message.guestsJson.length !== 0) {
      obj.guestsJson = base64FromBytes(message.guestsJson);
    }
    if (message.paymentJson.length !== 0) {
      obj.paymentJson = base64FromBytes(message.paymentJson);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BookHotelRequest>, I>>(base?: I): BookHotelRequest {
    return BookHotelRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BookHotelRequest>, I>>(object: I): BookHotelRequest {
    const message = createBaseBookHotelRequest();
    message.offerId = object.offerId ?? "";
    message.guestsJson = object.guestsJson ?? new Uint8Array(0);
    message.paymentJson = object.paymentJson ?? new Uint8Array(0);
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseBookHotelResponse(): BookHotelResponse {
  return { rawJson: new Uint8Array(0) };
}

export const BookHotelResponse: MessageFns<BookHotelResponse> = {
  encode(message: BookHotelResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rawJson.length !== 0) {
      writer.uint32(10).bytes(message.rawJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BookHotelResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBookHotelResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rawJson = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BookHotelResponse {
    return { rawJson: isSet(object.rawJson) ? bytesFromBase64(object.rawJson) : new Uint8Array(0) };
  },

  toJSON(message: BookHotelResponse): unknown {
    const obj: any = {};
    if (message.rawJson.length !== 0) {
      obj.rawJson = base64FromBytes(message.rawJson);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BookHotelResponse>, I>>(base?: I): BookHotelResponse {
    return BookHotelResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BookHotelResponse>, I>>(object: I): BookHotelResponse {
    const message = createBaseBookHotelResponse();
    message.rawJson = object.rawJson ?? new Uint8Array(0);
    return message;
  },
};

function createBaseBookTransferRequest(): BookTransferRequest {
  return { offerId: "", travelersJson: new Uint8Array(0), paymentJson: new Uint8Array(0), userId: 0 };
}

export const BookTransferRequest: MessageFns<BookTransferRequest> = {
  encode(message: BookTransferRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.offerId !== "") {
      writer.uint32(10).string(message.offerId);
    }
    if (message.travelersJson.length !== 0) {
      writer.uint32(18).bytes(message.travelersJson);
    }
    if (message.paymentJson.length !== 0) {
      writer.uint32(26).bytes(message.paymentJson);
    }
    if (message.userId !== 0) {
      writer.uint32(32).int64(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BookTransferRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBookTransferRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.offerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.travelersJson = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.paymentJson = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.userId = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BookTransferRequest {
    return {
      offerId: isSet(object.offerId) ? globalThis.String(object.offerId) : "",
      travelersJson: isSet(object.travelersJson) ? bytesFromBase64(object.travelersJson) : new Uint8Array(0),
      paymentJson: isSet(object.paymentJson) ? bytesFromBase64(object.paymentJson) : new Uint8Array(0),
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: BookTransferRequest): unknown {
    const obj: any = {};
    if (message.offerId !== "") {
      obj.offerId = message.offerId;
    }
    if (message.travelersJson.length !== 0) {
      obj.travelersJson = base64FromBytes(message.travelersJson);
    }
    if (message.paymentJson.length !== 0) {
      obj.paymentJson = base64FromBytes(message.paymentJson);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BookTransferRequest>, I>>(base?: I): BookTransferRequest {
    return BookTransferRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BookTransferRequest>, I>>(object: I): BookTransferRequest {
    const message = createBaseBookTransferRequest();
    message.offerId = object.offerId ?? "";
    message.travelersJson = object.travelersJson ?? new Uint8Array(0);
    message.paymentJson = object.paymentJson ?? new Uint8Array(0);
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseBookTransferResponse(): BookTransferResponse {
  return { rawJson: new Uint8Array(0) };
}

export const BookTransferResponse: MessageFns<BookTransferResponse> = {
  encode(message: BookTransferResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rawJson.length !== 0) {
      writer.uint32(10).bytes(message.rawJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BookTransferResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBookTransferResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rawJson = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BookTransferResponse {
    return { rawJson: isSet(object.rawJson) ? bytesFromBase64(object.rawJson) : new Uint8Array(0) };
  },

  toJSON(message: BookTransferResponse): unknown {
    const obj: any = {};
    if (message.rawJson.length !== 0) {
      obj.rawJson = base64FromBytes(message.rawJson);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BookTransferResponse>, I>>(base?: I): BookTransferResponse {
    return BookTransferResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BookTransferResponse>, I>>(object: I): BookTransferResponse {
    const message = createBaseBookTransferResponse();
    message.rawJson = object.rawJson ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
